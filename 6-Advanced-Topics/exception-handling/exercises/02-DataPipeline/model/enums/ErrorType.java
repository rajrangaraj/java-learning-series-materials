/**
 * Types of errors that can occur during processing
 */
public enum ErrorType {
    SOURCE_ERROR,
    TRANSFORMATION_ERROR,
    VALIDATION_ERROR,
    SINK_ERROR,
    UNEXPECTED_ERROR,
    HIGH_ERROR_RATE
} 